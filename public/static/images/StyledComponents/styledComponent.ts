import styled from 'styled-components';

export const White = styled.g`
  color = var(--white)
`;

export const Black = styled.g`
  color = var(--black)
`;

export const Red = styled.g`
  color= var(--red)
`;